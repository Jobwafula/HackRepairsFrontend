// Generated by CodiumAI

describe('Hero', () => {

    // Renders Hero component without crashing
    it('should render Hero component without crashing', () => {
      const { container } = render(<Hero />);
      expect(container).toBeInTheDocument();
    });

    // Handles missing image gracefully
    it('should handle missing image gracefully', () => {
      const { getByAltText } = render(<Hero />);
      const image = getByAltText('Phone Repair');
      expect(image).toBeInTheDocument();
      expect(image).toHaveAttribute('src', '/repair.png');
    });
});
